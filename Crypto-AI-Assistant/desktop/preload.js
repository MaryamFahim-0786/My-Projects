// Runs in an isolated context with access to Node + the page.
// Nothing needed yet, but this is where you'd expose safe APIs via
// contextBridge.exposeInMainWorld(...) if the desktop app needs native
// features (native notifications, tray icon, etc).
